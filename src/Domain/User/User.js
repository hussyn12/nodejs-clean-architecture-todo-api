class User {
    constructor(userId, name, email, password) {
        this.userId = userId
        this.name = name
        this.email = email
        this.password = password
    }
}

export default User